name: overlay_banner
description: A Reusable Flutter Widget for an Overlay Banner
version: 1.0.0
homepage:

environment:
  sdk: ">=2.14.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  integration_test:
    sdk: flutter
